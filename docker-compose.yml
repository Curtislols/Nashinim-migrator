services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Playwright needs /dev/shm to be large enough for Chromium
    shm_size: "1gb"
